#include "main.h"
/**
*_abs - print absolute value
*
*@a: integer value
*
*Return: returns 0 if prog runs smoothly
*/

int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (a * -1);
	}
}
